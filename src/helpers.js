/*
- export function capitalize with parameter word

    - returns word charAt(0) toUpperCase() +(plus) word slice(1)
*/
export function capitalize(word) {

    return word.charAt(0).toUpperCase() + word.slice(1);

}